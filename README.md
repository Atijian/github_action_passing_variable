# github_action_passing_variable
TF_VAR . This GitHub repo is another step forward to the terraform training. We will create a variables.tf file and fetch the values to these variable based on our input . 
